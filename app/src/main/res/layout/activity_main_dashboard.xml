<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/colorBackground">

    <!-- ── Colored header band ──────────────────────────────────── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_header_band"
        android:paddingTop="40dp"
        android:paddingBottom="28dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp">

        <!-- Icon + App name row + dark mode toggle -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="6dp">

            <ImageView
                android:layout_width="34dp"
                android:layout_height="34dp"
                android:src="@drawable/ic_traffic_alert"
                android:contentDescription="@string/header_icon_desc"
                android:layout_marginEnd="12dp" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/app_name"
                android:textAppearance="?attr/textAppearanceDisplaySmall"
                android:textColor="#FFFFFF"
                android:textStyle="bold" />

            <!-- Dark mode toggle -->
            <ImageButton
                android:id="@+id/button_toggle_dark_mode"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_dark_mode_toggle"
                android:contentDescription="@string/btn_toggle_dark_mode"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:padding="12dp" />

        </LinearLayout>

        <!-- Subtitle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/subtitle_text"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="#CCFFFFFF" />

    </LinearLayout>

    <!-- ── Main action button (pulls up into the header via negative margin) ── -->
    <Button
        android:id="@+id/button_open_reporter"
        android:layout_width="match_parent"
        tools:ignore="ButtonTooWide"
        android:layout_height="56dp"
        android:layout_marginTop="-28dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:text="@string/btn_create_report"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:textAppearance="?attr/textAppearanceLabelLarge"
        android:elevation="6dp"
        app:backgroundTint="?attr/colorSecondary"
        app:cornerRadius="28dp"
        android:textColor="?attr/colorOnSecondary" />

    <!-- ── Body content ─────────────────────────────────────────── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="36dp"
        android:paddingStart="20dp"
        android:paddingEnd="20dp">

        <!-- Section label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/section_latest_report"
            android:textAppearance="?attr/textAppearanceLabelMedium"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:letterSpacing="0.1"
            android:layout_marginBottom="10dp" />

        <!-- Report summary card -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/card_latest_report"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardCornerRadius="16dp"
            app:cardElevation="0dp"
            app:strokeWidth="1dp"
            app:strokeColor="?attr/colorOutlineVariant"
            app:cardBackgroundColor="?attr/colorSurface">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <!-- Card header -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/card_report_header"
                    android:textAppearance="?attr/textAppearanceTitleSmall"
                    android:textColor="?attr/colorPrimary"
                    android:textStyle="bold"
                    app:drawableStartCompat="@drawable/ic_traffic_alert"
                    android:drawablePadding="8dp"
                    app:drawableTint="?attr/colorPrimary"
                    android:gravity="center_vertical"
                    android:layout_marginBottom="10dp" />

                <!-- Report summary text -->
                <TextView
                    android:id="@+id/text_view_main_output"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/no_report_data"
                    android:textAppearance="?attr/textAppearanceBodyMedium"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:lineSpacingMultiplier="1.4"
                    android:freezesText="true" />

            </LinearLayout>
        </com.google.android.material.card.MaterialCardView>

    </LinearLayout>

</LinearLayout>
